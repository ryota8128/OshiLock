# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
# .terraform.lock.hcl はコミットする（プロバイダーバージョン固定のため）
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Keys (秘密鍵)
keys/*-private.pem
